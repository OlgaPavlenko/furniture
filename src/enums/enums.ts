export enum navBarLink {
  catalog = 'Catalog',
  aboutUs = 'About Us',
  delivery = 'Delivery',
}

export enum language {
  eng = 'en',
  ru = 'ru',
}

export enum searchType {
  text = 'text',
  search = 'search',
}
